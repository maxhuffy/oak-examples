export const regexes = {
  time_hhmm: /^(\d|0\d|1\d|2[0-3]):[0-5]\d$/,
  uuid: /^[0-9a-f]{8}-[0-9a-f]{4}-[0-5][0-9a-f]{3}-[089ab][0-9a-f]{3}-[0-9a-f]{12}$/i,
};

export const isRegex = (regex: keyof typeof regexes, str: string) => {
  return regexes[regex].test(str);
};
