export const GB = 1024 ** 3;
export const MB = 1024 ** 2;

export const bytesToGB = (bytes: number | bigint | null | undefined) => {
  if (bytes == null) return 0;
  return Number(bytes) / GB;
};

export { v4 as uuidv4 } from 'uuid';

export const noop = () => {};
