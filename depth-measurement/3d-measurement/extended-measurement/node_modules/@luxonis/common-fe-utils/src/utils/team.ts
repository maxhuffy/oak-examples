export const TEAM_SLUG_REGEX = /^[a-z-]{3,56}$/;

export function teamNameToTeamSlug(teamName: string): string {
  return teamName
    .toLowerCase()
    .replaceAll(/\d/g, '')
    .replaceAll(/[^a-z ]/g, '')
    .trim()
    .replaceAll(' ', '-');
}
