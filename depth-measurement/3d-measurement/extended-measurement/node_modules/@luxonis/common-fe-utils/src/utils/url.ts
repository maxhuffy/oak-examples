export type RedirectProps = {
  to: string;
  newTab?: boolean;
};

export function redirect(props: RedirectProps) {
  const { to, newTab = false } = props;

  if (newTab) {
    return window.open(to, '_blank');
  } else {
    window.location.assign(to);
    return null;
  }
}
